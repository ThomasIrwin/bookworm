# Copy to .env and adjust. Real environment variables take precedence over this file.

# SQLAlchemy URL. Use host bookworm-db inside the Docker network, localhost when running uvicorn directly.
DATABASE_URL=postgresql+asyncpg://bookworm_dev:dev_pwd@localhost:5432/bookworm
SERVER_DEV_PORT=8080

# Optional overrides (defaults shown)
# AUTH0_ISSUER=https://dev-ulixqg71ihyco2h1.us.auth0.com/
# AUTH0_AUDIENCE=http://localhost:8080/api/v1
# CORS_ALLOWED_ORIGINS=["http://localhost:3000"]
# ENABLE_DOCS=false
